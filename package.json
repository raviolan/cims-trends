{
  "name": "cims-dash-prototype",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "smoke": "node scripts/smoke-check.mjs",
    "check": "node --check app.js && node --check server.js && node --check src/*.js && node --check src/data/*.js && node --check scripts/*.mjs && npm run lint && npm run smoke"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "eslint": "^9.39.1",
    "prettier": "^3.6.2"
  }
}
